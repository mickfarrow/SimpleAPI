trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  - name: solution
    value: '**/*.sln'
  - name : buildPlatform 
    value: 'Any CPU'
  - name: buildConfiguration
    value: 'Release' 
  - group: "Shared"
  - name: va
    value: $[variables.shared_value1]
  

stages:
- stage: Build
  jobs:
  - job: BuildJob
    steps:
    - script:  "echo ${{ variables.solution }}"
    - script: "echo My secret is '$(secretvalue)'!"
    - script: "echo My secret is '$(secretvalue)'!"
    - script: "echo My secret is '$(secretvalue)'!"
    - script: "echo My shared secret is '$(va)'!"